export enum UploadMethod {
  Upload = 'upload',
  GitHub = 'github',
}

export enum Ownership {
  Leave = 'leave',
  Own = 'own',
}

export enum CacheControl {
  NoCache = 'no-cache',
  OneDay = '1 Day',
  OneWeek = '1 Week',
  OneMonth = '1 Month',
  OneYear = '1 Year'
}
