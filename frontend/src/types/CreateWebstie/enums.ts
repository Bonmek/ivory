export enum UploadMethod {
  Upload = 'upload',
  GitHub = 'github',
}

export enum Ownership {
  Leave = 'leave',
  Own = 'own',
}

export enum CacheControl {
  NoCache = '0',
  OneDay = '1',
  OneWeek = '7',
  OneMonth = '30',
  OneYear = '365'
}
