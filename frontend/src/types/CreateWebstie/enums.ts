export enum UploadMethod {
  Upload = 'upload',
  GitHub = 'github',
}

export enum Ownership {
  Leave = 'leave',
  Own = 'own',
}

export enum CacheControl {
  NoCache = 'no-cache',
  Public = 'public',
  Private = 'private',
}
